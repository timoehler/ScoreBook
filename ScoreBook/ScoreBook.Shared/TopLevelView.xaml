<Page
    x:Class="ScoreBook.TopLevelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ScoreBook"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
	<Page.Resources>
		<Style TargetType="TextBlock">
			<Style.Setters>
				<Setter Property="Foreground" Value="#FF585955"/>
			</Style.Setters>
		</Style>
		<Style TargetType="Button">
			<Style.Setters>
				<Setter Property="BorderThickness" Value="1"/>
			</Style.Setters>
		</Style>


	</Page.Resources>

	<Grid Background="#FFF0DFDD">
		<UserControl HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Content="{Binding CurrentView}"/>
		<Rectangle HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Fill="#80808080" Visibility="{Binding MessageVisibility}"/>
		<UserControl HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding MessageVisibility}" Content="{Binding MessageView}"/>
    </Grid>
</Page>
